const initialState = {
    categoryArr: [{name:"wedding",id:1},{name:"trip",id:2},{name:"shopping",id:3}]
}
const categoryReducer = (state = initialState) => {
    return state;
}

export default categoryReducer;
